Minimal bash script for detecting failed login attempts.

## Features
- Counts failed/invalid login attempts by IP
- Simple grep-based parsing
- Console alerts + file logging
- No external dependencies
